name: gh-pages

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Clone repo
        uses: actions/checkout@v1
         
      - name: Installing dependencies
        run: yarn       

      - name: Deploy to gh-pages
        run: yarn deploy